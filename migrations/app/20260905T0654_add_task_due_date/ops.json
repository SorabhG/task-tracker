[
  {
    "id": "column.public.task.dueDate",
    "label": "Add column \"dueDate\" to \"task\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "dueDate",
        "table": "task"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"dueDate\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "task",
          "dueDate"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"dueDate\"",
        "sql": "ALTER TABLE \"public\".\"task\" ADD COLUMN \"dueDate\" timestamptz"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"dueDate\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "task",
          "dueDate"
        ]
      }
    ]
  }
]